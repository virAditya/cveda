[project]
name = "cveda"
version = "0.1.0"
description = "Lightweight EDA utilities for computer vision datasets"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
    { name = "Aditya" }
]